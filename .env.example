PORT=3847
BASE_URL=http://localhost:3847
STORE_NAME=PixelForge AI

# === STRIPE (connect your bank for payouts) ===
# 1. https://dashboard.stripe.com/register
# 2. Complete business profile → Settings → Bank accounts → Add bank
# 3. Developers → API keys → Secret key
STRIPE_SECRET_KEY=sk_test_xxxxxxxx
# Webhook signing secret (after creating webhook endpoint)
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxx

# === AI generation (optional — real photoreal images/video) ===
# https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_xxxxxxxx

ADMIN_TOKEN=change-me-to-a-long-random-string
