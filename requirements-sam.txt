# ============================================================
# requirements-sam.txt — Sam local agent dependencies
# Install: pip install -r requirements-sam.txt
# ============================================================

# ── Agent Framework ─────────────────────────────────────────
langchain>=0.3.0
langchain-ollama>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.0
langsmith>=0.1.0          # optional tracing — set LANGCHAIN_TRACING_V2=true

# ── Telegram Bot ─────────────────────────────────────────────
python-telegram-bot[job-queue]>=21.0

# ── Async SQLite ─────────────────────────────────────────────
aiosqlite>=0.20.0

# ── Vector similarity (pure-numpy fallback) ──────────────────
numpy>=1.26.0             # likely already installed via base requirements

# ── Embeddings (reuses fastembed from base requirements.txt) ─
# fastembed is already in requirements.txt → no duplicate

# ── Voice ────────────────────────────────────────────────────
pvporcupine>=3.0.0        # wake word — needs picovoice.ai API key
pyaudio>=0.2.14           # microphone capture & playback
sounddevice>=0.4.7        # audio output (Piper TTS playback)

# ── Web / HTTP ───────────────────────────────────────────────
# fastapi, uvicorn, httpx, python-dotenv already in requirements.txt
websockets>=12.0          # WebSocket server for dashboard voice streaming
aiofiles>=23.0            # async file I/O for tools

# ── System info ──────────────────────────────────────────────
psutil>=5.9.0             # CPU/RAM/disk/battery for get_system_info tool

# ── Cron parsing ─────────────────────────────────────────────
# croniter already in requirements.txt

# ── Browser automation (browse tool) ─────────────────────────
playwright>=1.40.0        # headless browser for browse() tool
# After install: python -m playwright install chromium

# ── Email (send_email tool) ──────────────────────────────────
# aiosmtplib for async SMTP; falls back to Telegram draft notification
aiosmtplib>=3.0.0

# ── Testing ──────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
respx>=0.20               # mock httpx calls in tests
